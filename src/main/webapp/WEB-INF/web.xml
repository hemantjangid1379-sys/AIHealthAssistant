<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
 
  <display-name>MedAI — AI Health Assistant</display-name>
 
  <!--
    ===================================================================
    ChatbotServlet — existing servlet (no changes needed here,
    the @WebServlet annotation in the Java file handles mapping)
    ===================================================================
  -->
 
  <!--
    NOTE: ChatbotServlet and SummaryServlet are both registered
    automatically via their @WebServlet annotations in the Java files.
    We do NOT register them here again — doing so causes a
    duplicate mapping error in Tomcat.
  -->
 
  <!-- Session timeout: 30 minutes -->
  <session-config>
    <session-timeout>30</session-timeout>
  </session-config>
 
  <!-- Default page -->
  <welcome-file-list>
    <welcome-file>home.jsp</welcome-file>
  </welcome-file-list>
 
</web-app>